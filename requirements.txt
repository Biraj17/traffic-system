# Smart Traffic Management System — Python dependencies
# eclipse-sumo ships the full SUMO binaries (sumo, sumo-gui, netconvert,
# tools) via pip; src/sumo_env.py auto-detects it, so no system SUMO or
# SUMO_HOME is required. traci/sumolib are the matching Python bindings.

eclipse-sumo>=1.27.0
traci>=1.18.0
sumolib>=1.18.0

scikit-learn>=1.4.0
joblib>=1.3.0
pandas>=2.1.0
numpy>=1.26.0

streamlit>=1.30.0
plotly>=5.18.0
matplotlib>=3.8.0
pyproj>=3.6.0  # lon/lat -> net meters for real place-name labels

pytest>=7.4.0
