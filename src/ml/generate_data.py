"""Run many automatic-mode simulation episodes with varied demand and log
per-cycle features (vehicle_count, waiting_time, queue_growth_rate,
peak/off-peak flag, max-waiting-lane index) plus the green time that produced
the best outcome, to data/training.csv.

Implemented in Phase 5.
"""
